//datatypes
"use strict";//new standards treats by the compiler
/* symbol       --makes unique objects for the variables
   string        -stores values
   number        --stores int values
   boolean        --true or false
   bigint        --large values
   null          --satnd alone value and also atype
   undefined      --value is not assigned
*/
console.log(typeof null)
const {muni} =req.body