//memory are two types 
//stack(primitive)->copy   ,  heap(nonprimitive )->adress
// in primiive copy value you pass so changes not appeared in original
// in nonprimitive changes appear in the original also because all point to same value so that you change one value it wiilreflect on th other